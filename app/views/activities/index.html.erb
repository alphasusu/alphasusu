<div class="row banner-offset">
	<div class="small-12 columns">
		<h1>Activities</h1>
		<p class="show-for-medium-up">Student Groups are a great way to get more out of University. Maybe you could compete in one of the <%= @sport_count %> sports that are on offer? Societies cover everything imaginable, but if your niche is missing then you could start one! If you want to perform then the Performing Arts groups offer a world of choice, or help out your union by joining one of our media teams!</p>
	</div>
</div>
<div class="row">
	<div class="medium-6 small-12 columns"><h2><strong><%= @society_count %></strong> <a href="#" onclick="selectKind('societies')">Societies</a></h2></div>
	<div class="medium-6 small-12 columns"><h2><strong><%= @sport_count %></strong> <a href="#" onclick="selectKind('sports')">Sports</a></h2></div>
	<div class="medium-6 small-12 columns"><h2><strong><%= @performing_art_count %></strong> <a href="#" onclick="selectKind('performing-arts')">Performing Arts</a></h2></div>
	<div class="medium-6 small-12 columns"><h2><strong><%= @media_group_count %></strong> <a href="#" onclick="selectKind('media')">Media Groups</a></h2></div>
</div>
<script>
	STUDENT_GROUPS = <%= raw @groups_json %>;
</script>
<script type="script/x-template" id="group-template">
	<div class="large-3 medium-4 small-6 columns left group">
		<div class="group-logo-wrapper">
			<a href="{{= url}}"><img src="{{= logo}}" alt="{{= name}}" class="group-logo"/></a>
		</div>
		<h5><a href="{{= url}}">{{= name}}</a></h5>
	</div>
</script>
<script type="script/x-template" id="groups-kinds-template">
	<dl class="sub-nav">
		<dt>Filter:</dt>
		{{ _.each(kinds, function(kind) { }}
			<dd><a id="{{= kind.toLowerCase().replace(' ', '-') }}-filter">{{= kind }}</a></dd>
		{{ }); }}
	</dl>
</script>
<hr/>
<div class="row" id="groups-browser">
	<div class="large-12 columns">
		<input type="text" placeholder="Search..." id="groups-filter-input" />
	</div>
	<div class="large-12 columns" id="groups-filter">
	</div>
</div>
<div class="row" id="groups-browser-list">
</div>