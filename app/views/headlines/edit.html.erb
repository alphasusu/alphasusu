<%= render 'shared/admin_form_page', 
	resource: @headline,
	resource_list_path: headlines_path,
	help_text: "<em>Title</em> and <em>Body</em> are both free-form HTML inputs. This means that they can contains links, images, and custom styles. The <em>title</em> will be shown as an <code>h2</code> tag, and the <em>body</em> as an <code>h3</code> tag.<br/><br/>Ensure that you test headlines on mobile devices as they display differently based on the size of screen they are viewed on." %>