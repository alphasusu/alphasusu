<%= form_for(@media_group) do |f| %>
  <% if @media_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@media_group.errors.count, "error") %> prohibited this media_group from being saved:</h2>

      <ul>
      <% @media_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
