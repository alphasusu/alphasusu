<%= form_for(@article) do |f| %>
  <div class="row">
    <div class="large-12 columns <%= field_error_class @article, :title %>">
      <%= f.label :title %>
      <%= f.text_field :title %>
      <%= field_error @article, :title, "Title" %>
    </div>
    <div class="large-12 columns <%= field_error_class @article, :body %>">
      <%= f.label :body %>
      <%= f.text_area :body %>
      <%= field_error @article, :body, "Post" %>
    </div>
    <div class="large-12 columns">
      <%= f.submit class: "button" %>
      <% if params[:action] == "edit" %>
        <%= link_to 'Destroy', @article, method: :delete, data: { confirm: 'Are you sure?' }, class: "button alert" %>
      <% end %>
    </div>
  </div>
<% end %>
